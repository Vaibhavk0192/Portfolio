// import { db } from "../utils/firebase";
// import { HomeInterface } from "../types/homeTypes";
// import { HomeData } from "../data/home";

// const uploadHome = async () => {
//   const docRef = await db.collection("home").add(HomeData as HomeInterface);
//   console.log("Home uploaded successfully with ID:", docRef.id);
// };

// uploadHome();